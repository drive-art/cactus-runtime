name: Build Cactus Android ARM64

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake build-essential python3 git unzip wget

      - name: Install Android NDK r26d
        run: |
          wget -q "https://dl.google.com/android/repository/android-ndk-r26d-linux.zip"
          unzip -q android-ndk-r26d-linux.zip
          echo "NDK=$PWD/android-ndk-r26d" >> $GITHUB_ENV

      - name: Clone Cactus
        run: git clone https://github.com/cactus-compute/cactus

      - name: Patch telemetry to remove curl dependency
        run: |
          cd cactus/cactus
          # Заменяем include curl на заглушку
          sed -i 's/#include <curl\/curl.h>/\/\/ curl disabled for Android/' telemetry/telemetry.cpp
          # Проверяем
          grep -n "curl" telemetry/telemetry.cpp | head -5

      - name: Build for Android ARM64
        run: |
          cd cactus/cactus
          mkdir -p build_android && cd build_android
          cmake .. \
            -DCMAKE_TOOLCHAIN_FILE=$NDK/build/cmake/android.toolchain.cmake \
            -DANDROID_ABI=arm64-v8a \
            -DANDROID_PLATFORM=android-26 \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_SHARED_LIBS=ON
          make -j4

      - name: Find library
        run: find cactus -name "*.so" | grep -v venv | head -10

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: libcactus-android-arm64
          path: cactus/cactus/build_android/**/*.so
