name: Build Cactus — Poco F1

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    name: poco-f1

    steps:
      - name: Checkout Cactus
        uses: actions/checkout@v4
        with:
          repository: cactus-compute/cactus
          ref: main
          path: cactus-src

      - name: Install dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y cmake ninja-build wget unzip

      - name: Download NDK r26d
        run: |
          wget -q https://dl.google.com/android/repository/android-ndk-r26d-linux.zip
          unzip -q android-ndk-r26d-linux.zip
          echo "NDK_PATH=$(pwd)/android-ndk-r26d" >> $GITHUB_ENV

      - name: Show structure
        run: |
          find cactus-src -name "telemetry*"
          find cactus-src/android -type f | head -20

      - name: Patch telemetry
        run: |
          TELEM=$(find cactus-src -name "telemetry.cpp" | head -1)
          if [ -f "$TELEM" ]; then
            echo 'namespace cactus { namespace telemetry { void init(const char*, const char*, const char*) {} }}' > "$TELEM"
          fi

      - name: Configure CMake
        run: |
          mkdir -p build-poco-f1
          cd build-poco-f1
          cmake ../cactus-src/android \
            -G Ninja \
            -DCMAKE_TOOLCHAIN_FILE="${NDK_PATH}/build/cmake/android.toolchain.cmake" \
            -DANDROID_ABI=arm64-v8a \
            -DANDROID_PLATFORM=android-26 \
            -DANDROID_STL=c++_shared \
            -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: |
          cd build-poco-f1
          ninja -j$(nproc)
          find . -name "*.so"

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: libcactus-poco-f1
          path: build-poco-f1/**/*.so
          retention-days: 30
